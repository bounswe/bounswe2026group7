<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/>
    </parent>

    <groupId>com.group7</groupId>
    <artifactId>backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>backend</name>
    <description>BounSwe2026 Group7 Backend</description>

    <properties>
        <java.version>21</java.version>
        <spring-ai.version>1.0.1</spring-ai.version>
    </properties>

    <!--
        Spring AI BOM pins the OpenAI starter + its transitive deps to one
        coherent line (1.0.x). Lets us swap providers later (Azure, Bedrock,
        Vertex) by changing one starter and one property without juggling
        individual versions. Required for the advanced mentor ranker (#436)
        — semantic embeddings + LLM prose explanations.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>com.resend</groupId>
            <artifactId>resend-java</artifactId>
            <version>3.1.0</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.11.5</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.11.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.0</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-reflect</artifactId>
            <version>2.1.20</version>
        </dependency>
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>8.0</version>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>8.10.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!-- Neo4j follow-graph mirror (#437). The Bolt driver bean is lazy
             (no startup connect) and the @EnableNeo4jRepositories scan is
             gated by app.recommendations.follow.sync.enabled, so this dep
             is dormant when sync is disabled. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-neo4j</artifactId>
        </dependency>
        <!-- Spring AI OpenAI starter — embeddings (#436 semantic match)
             AND chat completions (LLM prose explanations). Single starter,
             two consumers. Without OPENAI_API_KEY the EmbeddingModel /
             ChatModel beans still form; calls fail at runtime and the
             services degrade gracefully. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>
        <!-- RFC 5545 (iCalendar) producer for the mentor-availability ICS export
             endpoint (#250). 4.x aligned with Java 21 and the jakarta namespace. -->
        <dependency>
            <groupId>org.mnode.ical4j</groupId>
            <artifactId>ical4j</artifactId>
            <version>4.0.6</version>
        </dependency>
        <!-- FCM transport for push notifications (#136). Initialised lazily by
             FcmConfig only when app.fcm.credentials-path resolves to a real
             service-account JSON file; absent that, NoOpPushDeliveryService is
             wired so dev/CI environments still boot. -->
        <dependency>
            <groupId>com.google.firebase</groupId>
            <artifactId>firebase-admin</artifactId>
            <version>9.4.1</version>
        </dependency>
        <!-- Faker generator for the profile-gated TestSupportController (#315).
             Used to seed users in CI E2E runs; the controller bean is loaded
             only when app.test-endpoints.enabled=true, so this dependency is
             never exercised in production. -->
        <dependency>
            <groupId>net.datafaker</groupId>
            <artifactId>datafaker</artifactId>
            <version>2.4.2</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers — used by #437 PR2 repo tests against real
             Postgres + Neo4j+GDS. junit-jupiter pulls in @Testcontainers /
             @Container; the postgresql + neo4j modules add the matching
             container classes for @ServiceConnection / explicit start. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>neo4j</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <configuration>
                    <excludes>
                        <exclude>*$HibernateProxy*</exclude>
                        <exclude>*$HibernateInstantiator*</exclude>
                        <exclude>*$MockitoMock*</exclude>
                        <exclude>*$$EnhancerByCGLIB$$*</exclude>
                        <exclude>*$$FastClassByCGLIB$$*</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                        <configuration>
                            <!--
                                ByteBuddy ships some attribute classes compiled
                                with class-file major-version 68 (Java 24); the
                                JaCoCo agent (ASM 9.x in 0.8.12) can't read
                                those and would otherwise blow up Mockito's
                                inline mocking under JDK ≤ 23. Excluding the
                                ByteBuddy package from instrumentation skips it
                                cleanly — no impact on coverage of our code.
                            -->
                            <excludes>
                                <exclude>net.bytebuddy.*</exclude>
                                <exclude>net.bytebuddy.**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                    <execution>
                        <!--
                            Coverage gate for the advanced-mentor-ranker work
                            (#436). Targets the classes introduced by this PR;
                            pre-existing files keep their current coverage
                            profile unchanged.

                            Thresholds (0.90 line / 0.80 branch) capture the
                            algorithmic surface in tests while leaving a
                            realistic margin for record synthetic methods
                            (equals/hashCode/toString) and defensive null-
                            guards on Spring AI bean providers — those
                            branches are unreachable from outside Spring AOT
                            and won't ever exercise without booting a real
                            context. Run with `mvn verify`.
                        -->
                        <id>advanced-ranker-check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>CLASS</element>
                                    <includes>
                                        <include>com.group7.backend.service.ranking.AdvancedMentorRanker</include>
                                        <include>com.group7.backend.service.ranking.MmrReranker</include>
                                        <include>com.group7.backend.service.ranking.MentorScoringPipeline</include>
                                        <include>com.group7.backend.service.ranking.signals.*</include>
                                        <include>com.group7.backend.service.embedding.SemanticSimilarityService</include>
                                        <include>com.group7.backend.service.embedding.EmbeddingHealthIndicator</include>
                                        <include>com.group7.backend.service.explanation.MatchExplanationService</include>
                                        <include>com.group7.backend.service.explanation.ChatHealthIndicator</include>
                                        <include>com.group7.backend.util.HaversineDistance</include>
                                    </includes>
                                    <excludes>
                                        <!-- Pure records: synthetic equals/hashCode/toString
                                             would need exercising for 100% branch coverage
                                             and provide no meaningful test value. -->
                                        <exclude>com.group7.backend.service.ranking.SignalContribution</exclude>
                                        <exclude>com.group7.backend.service.ranking.ScoringContext</exclude>
                                    </excludes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.90</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                    <execution>
                        <!--
                            Coverage gate for the advanced follow-recommendation
                            packages (#437). Numbers reflect actual coverage with
                            a small headroom (~5pp) so a refactor moving a branch
                            doesn't fail the build, but a deleted test surfaces.

                            service.ranking.graph is lower because PPR + projection
                            paths include defensive driver-failure branches and an
                            EDGE_COUNT_WARN_THRESHOLD that triggers only above 1M
                            edges (not practical to fixture).

                            Coexists with advanced-ranker-check above; both rules
                            must pass before the build greens.
                        -->
                        <id>check-pr2-branch-coverage</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <haltOnFailure>true</haltOnFailure>
                            <rules>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.group7.backend.service.ranking</include>
                                        <include>com.group7.backend.service.ranking.follow</include>
                                        <include>com.group7.backend.service.ranking.coldstart</include>
                                        <include>com.group7.backend.service.graph</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.65</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.group7.backend.service.ranking.graph</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.55</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
